---
phase: 01-security-foundation-deploy-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/security.py
  - backend/requirements.txt
  - backend/tests/conftest.py
  - backend/tests/test_security.py
autonomous: true
requirements: [SEC-06]

must_haves:
  truths:
    - "python-jose is not in requirements.txt — PyJWT 2.12+ is"
    - "passlib is not in requirements.txt — direct bcrypt calls are used"
    - "All 268+ existing tests pass with new libraries"
    - "Existing passlib-generated password hashes verify with direct bcrypt"
    - "Token encode/decode works identically (same payload structure per D-03)"
  artifacts:
    - path: "backend/app/core/security.py"
      provides: "JWT encode/decode via PyJWT, password hash/verify via direct bcrypt"
      contains: "import jwt"
    - path: "backend/requirements.txt"
      provides: "Updated dependencies without python-jose or passlib"
      contains: "PyJWT"
    - path: "backend/tests/test_security.py"
      provides: "Unit tests for security.py functions"
      exports: ["test_passlib_bcrypt_compat", "test_decode_tampered_token", "test_access_token_has_expected_claims"]
  key_links:
    - from: "backend/app/core/security.py"
      to: "jwt (PyJWT)"
      via: "import jwt; jwt.encode / jwt.decode"
      pattern: "import jwt"
    - from: "backend/app/core/security.py"
      to: "bcrypt"
      via: "import bcrypt; bcrypt.hashpw / bcrypt.checkpw"
      pattern: "import bcrypt"
    - from: "backend/tests/conftest.py"
      to: "backend/app/core/security.py"
      via: "from app.core.security import create_access_token"
      pattern: "create_access_token"
---

<objective>
Replace python-jose (CVE-2024-33663) with PyJWT 2.12+ and passlib with direct bcrypt calls in the security module.

Purpose: Eliminate known CVE in JWT library and remove abandoned dependency. This is the foundation for all subsequent auth changes in this phase.
Output: Updated security.py with PyJWT + bcrypt, updated requirements.txt, new test_security.py with unit tests, all existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-foundation-deploy-fix/01-CONTEXT.md
@.planning/phases/01-security-foundation-deploy-fix/01-RESEARCH.md

@backend/app/core/security.py
@backend/requirements.txt
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_security.py with unit tests for security module + passlib compat test</name>
  <files>backend/tests/test_security.py</files>
  <read_first>
    backend/app/core/security.py
    backend/tests/conftest.py
    backend/requirements.txt
  </read_first>
  <behavior>
    - test_passlib_bcrypt_compat: a hash created by passlib CryptContext(schemes=["bcrypt"]) verifies with bcrypt.checkpw — assert passlib_hash.startswith("$2b$") and bcrypt.checkpw succeeds
    - test_hash_password_returns_bcrypt_string: hash_password("test") returns a string starting with "$2b$"
    - test_verify_password_correct: verify_password("test", hash_of_test) returns True
    - test_verify_password_wrong: verify_password("wrong", hash_of_test) returns False
    - test_create_access_token_has_expected_claims: create_access_token(uuid, uuid, "admin") produces a JWT with sub, tenant_id, role, exp, type="access" claims
    - test_decode_token_valid: decode_token(valid_token) returns correct payload dict
    - test_decode_tampered_token: decode_token(token + "x") raises ValueError
    - test_decode_expired_token: a token with exp in the past raises ValueError
  </behavior>
  <action>
    Create `backend/tests/test_security.py` with the tests listed in behavior. Per D-02, include a one-time passlib compatibility test that:
    1. Imports passlib CryptContext, creates a hash with schemes=["bcrypt"]
    2. Asserts the hash starts with "$2b$"
    3. Asserts bcrypt.checkpw(password.encode(), passlib_hash.encode()) returns True

    For the token tests, use `create_access_token` and `decode_token` from `app.core.security`. Use a known UUID for subject and tenant_id. For the expired token test, create a token with `expires_delta=timedelta(seconds=-1)`.

    These tests must pass against the CURRENT security.py (before migration) to establish a baseline, then continue passing after the PyJWT migration in Task 2.

    Note: The passlib compat test imports passlib directly — this is intentional; it validates format compatibility before removal. After confirming, this test can be marked with `@pytest.mark.skipif` or removed in a future cleanup, but for now it serves as proof.
  </action>
  <verify>
    <automated>cd /home/spro/development/VERA/backend && python3 -m pytest tests/test_security.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_security.py exists and contains `def test_passlib_bcrypt_compat`
    - backend/tests/test_security.py contains `def test_hash_password_returns_bcrypt_string`
    - backend/tests/test_security.py contains `def test_decode_tampered_token`
    - backend/tests/test_security.py contains `def test_decode_expired_token`
    - `python3 -m pytest tests/test_security.py -v` shows all tests PASSED (exit code 0)
  </acceptance_criteria>
  <done>All 8 security unit tests pass against current (pre-migration) code, establishing the baseline</done>
</task>

<task type="auto">
  <name>Task 2: Migrate security.py from python-jose/passlib to PyJWT/bcrypt and update requirements.txt</name>
  <files>backend/app/core/security.py, backend/requirements.txt, backend/tests/conftest.py</files>
  <read_first>
    backend/app/core/security.py
    backend/requirements.txt
    backend/tests/conftest.py
    backend/app/api/v1/auth.py
  </read_first>
  <action>
    **Step 1: Update requirements.txt** (per D-01, D-02):
    - Replace line `python-jose[cryptography]==3.3.0` with `PyJWT>=2.12,<3.0`
    - Replace line `passlib[bcrypt]==1.7.4` with nothing (remove it)
    - Keep `bcrypt==4.0.1` (already present, now a direct dependency instead of transitive)
    - Run `pip install -r requirements.txt` to install PyJWT into venv

    **Step 2: Rewrite security.py** (per D-01, D-02, D-03):

    Replace the imports:
    ```python
    # OLD:
    from jose import jwt, JWTError
    from passlib.context import CryptContext
    # NEW:
    import jwt  # PyJWT
    import bcrypt
    ```

    Remove the `pwd_context` line entirely.

    Replace `hash_password`:
    ```python
    def hash_password(password: str) -> str:
        return bcrypt.hashpw(password.encode("utf-8"), bcrypt.gensalt()).decode("utf-8")
    ```

    Replace `verify_password`:
    ```python
    def verify_password(plain_password: str, hashed_password: str) -> bool:
        return bcrypt.checkpw(
            plain_password.encode("utf-8"),
            hashed_password.encode("utf-8"),
        )
    ```

    In `decode_token`, change the exception catch:
    ```python
    except jwt.PyJWTError as e:
        raise ValueError(f"Invalid token: {e}")
    ```

    **All other functions remain UNCHANGED** — `create_access_token`, `create_refresh_token`, `create_superadmin_challenge_token`, `create_superadmin_token` use `jwt.encode()` which has the same API in PyJWT. The `algorithm=settings.ALGORITHM` and `algorithms=[settings.ALGORITHM]` calls are identical. Per D-03, token payload structure (sub, tenant_id, role, exp, type) is unchanged.

    **Step 3: Update conftest.py** — no changes needed to the fixture calls themselves since `create_access_token` signature is unchanged. However, verify the import still works: `from app.core.security import hash_password, create_access_token`.

    **Step 4: Run full test suite** to confirm no regressions.
  </action>
  <verify>
    <automated>cd /home/spro/development/VERA/backend && pip install -r requirements.txt -q && python3 -m pytest tests/ -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/core/security.py contains `import jwt` (NOT `from jose import jwt`)
    - backend/app/core/security.py contains `import bcrypt` (NOT `from passlib`)
    - backend/app/core/security.py contains `jwt.PyJWTError` in the except clause
    - backend/app/core/security.py contains `bcrypt.hashpw(` and `bcrypt.checkpw(`
    - backend/app/core/security.py contains `.decode("utf-8")` after hashpw call
    - backend/requirements.txt contains `PyJWT>=2.12`
    - backend/requirements.txt does NOT contain `python-jose`
    - backend/requirements.txt does NOT contain `passlib`
    - backend/requirements.txt contains `bcrypt==4.0.1`
    - Full test suite (`python3 -m pytest tests/ -q`) exits with code 0 (all 268+ tests pass)
  </acceptance_criteria>
  <done>python-jose and passlib fully removed, PyJWT and direct bcrypt in use, all existing tests pass, CVE-2024-33663 eliminated</done>
</task>

</tasks>

<verification>
- `grep -c "python-jose\|passlib" backend/requirements.txt` returns 0
- `grep "import jwt" backend/app/core/security.py` returns `import jwt`
- `grep "import bcrypt" backend/app/core/security.py` returns `import bcrypt`
- `cd backend && python3 -m pytest tests/ -q` — all tests pass (268+ including new test_security.py tests)
- `cd backend && python3 -m pytest tests/test_security.py -v` — all 8 security unit tests pass
</verification>

<success_criteria>
- python-jose CVE-2024-33663 is eliminated — library removed from requirements.txt
- passlib removed — direct bcrypt is the only password hashing mechanism
- Token payload structure unchanged (per D-03) — existing tokens continue to decode
- All existing tests pass without modification (except test_security.py which is new)
- Passlib hash compatibility explicitly verified by test_passlib_bcrypt_compat
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-foundation-deploy-fix/01-01-SUMMARY.md`
</output>
