---
phase: 03-audit-trail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/audit_service.py
  - backend/app/schemas/audit_log.py
  - backend/alembic/versions/k5l6m7n8o9p0_audit_log_indexes_and_revoke.py
  - backend/tests/test_audit.py
autonomous: true
requirements: [AUDIT-01, AUDIT-05]

must_haves:
  truths:
    - "AuditLog table has composite index on (tenant_id, entity_type, entity_id)"
    - "AuditLog table has composite index on (tenant_id, created_at)"
    - "PostgreSQL REVOKE migration skips on SQLite without error"
    - "audit_service.write() stages an AuditLog row without committing"
  artifacts:
    - path: "backend/app/services/audit_service.py"
      provides: "Shared audit write helper"
      exports: ["write"]
    - path: "backend/app/schemas/audit_log.py"
      provides: "Pydantic schemas for audit log API"
      exports: ["AuditLogOut", "AuditLogPageOut"]
    - path: "backend/alembic/versions/k5l6m7n8o9p0_audit_log_indexes_and_revoke.py"
      provides: "Alembic migration for indexes + REVOKE"
      contains: "ix_audit_log_tenant_entity"
    - path: "backend/tests/test_audit.py"
      provides: "Test scaffolds for all AUDIT requirements"
      min_lines: 50
  key_links:
    - from: "backend/app/services/audit_service.py"
      to: "backend/app/models/audit.py"
      via: "imports AuditLog model"
      pattern: "from app\\.models\\.audit import AuditLog"
    - from: "backend/alembic/versions/k5l6m7n8o9p0_audit_log_indexes_and_revoke.py"
      to: "audit_log table"
      via: "create_index + REVOKE"
      pattern: "ix_audit_log_tenant_entity"
---

<objective>
Create the audit trail foundation: shared audit_service.write() helper, Pydantic schemas, Alembic migration for composite indexes + REVOKE, and test scaffolds for the entire phase.

Purpose: All subsequent plans depend on audit_service.write() existing and the test file being scaffolded. The indexes ensure the admin UI queries perform well. The REVOKE makes audit_log append-only at DB level.
Output: audit_service.py, audit_log schemas, Alembic migration, test_audit.py with scaffolds
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-audit-trail/03-RESEARCH.md
@.planning/phases/03-audit-trail/03-VALIDATION.md

@backend/app/models/audit.py
@backend/app/api/v1/shifts.py (lines 41-52 for _write_audit pattern)
@backend/tests/conftest.py

<interfaces>
<!-- Existing AuditLog model from backend/app/models/audit.py -->
class AuditLog(Base):
    __tablename__ = "audit_log"
    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    tenant_id: Mapped[uuid.UUID | None] = mapped_column(ForeignKey("tenants.id"), nullable=True)
    user_id: Mapped[uuid.UUID | None] = mapped_column(ForeignKey("users.id"), nullable=True)
    entity_type: Mapped[str] = mapped_column(String(100), nullable=False)
    entity_id: Mapped[uuid.UUID | None] = mapped_column(nullable=True)
    action: Mapped[str] = mapped_column(String(50), nullable=False)
    old_values: Mapped[dict | None] = mapped_column(JSON, nullable=True)
    new_values: Mapped[dict | None] = mapped_column(JSON, nullable=True)
    ip_address: Mapped[str | None] = mapped_column(String(50), nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=...)

<!-- Current Alembic HEAD -->
down_revision = "j4k5l6m7n8o9"

<!-- Existing _write_audit in shifts.py (lines 41-52) -->
async def _write_audit(db, *, tenant_id, user_id, entity_id, action: str,
                        old_values: dict | None = None, new_values: dict | None = None):
    log = AuditLog(tenant_id=tenant_id, user_id=user_id, entity_type="shift",
                    entity_id=entity_id, action=action,
                    old_values=old_values, new_values=new_values)
    db.add(log)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audit_service.py, Pydantic schemas, and Alembic migration</name>
  <files>
    backend/app/services/audit_service.py,
    backend/app/schemas/audit_log.py,
    backend/alembic/versions/k5l6m7n8o9p0_audit_log_indexes_and_revoke.py
  </files>
  <read_first>
    backend/app/models/audit.py,
    backend/app/api/v1/shifts.py (lines 41-52),
    backend/alembic/versions/j4k5l6m7n8o9_add_token_version.py
  </read_first>
  <action>
1. Create `backend/app/services/audit_service.py` with a single async function `write()`:
   - Signature: `async def write(db: AsyncSession, *, tenant_id: uuid.UUID, user_id: uuid.UUID, entity_type: str, entity_id: uuid.UUID, action: str, old_values: dict | None = None, new_values: dict | None = None) -> None`
   - Body: instantiate `AuditLog(tenant_id=tenant_id, user_id=user_id, entity_type=entity_type, entity_id=entity_id, action=action, old_values=old_values, new_values=new_values)` and call `db.add(log)`
   - DO NOT call `await db.commit()` inside this function — the caller owns the transaction
   - Import `AuditLog` from `app.models.audit`
   - Import `AsyncSession` from `sqlalchemy.ext.asyncio`
   - Import `uuid` from stdlib

2. Create `backend/app/schemas/audit_log.py` with two Pydantic v2 models:
   - `AuditLogOut`: fields `id: uuid.UUID`, `tenant_id: uuid.UUID | None`, `user_id: uuid.UUID | None`, `entity_type: str`, `entity_id: uuid.UUID | None`, `action: str`, `old_values: dict | None`, `new_values: dict | None`, `created_at: datetime`. Set `model_config = {"from_attributes": True}`
   - `AuditLogPageOut`: fields `items: list[AuditLogOut]`, `total: int`

3. Create Alembic migration `backend/alembic/versions/k5l6m7n8o9p0_audit_log_indexes_and_revoke.py`:
   - `revision = "k5l6m7n8o9p0"`
   - `down_revision = "j4k5l6m7n8o9"`
   - In `upgrade()`:
     a. Use `Inspector.from_engine(conn)` to get existing index names on `audit_log`
     b. Create index `ix_audit_log_tenant_entity` on `["tenant_id", "entity_type", "entity_id"]` if not exists
     c. Create index `ix_audit_log_tenant_created` on `["tenant_id", "created_at"]` if not exists
     d. Check `conn.dialect.name == "postgresql"` — if true, execute `REVOKE UPDATE, DELETE ON audit_log FROM vera`
   - In `downgrade()`:
     a. Drop both indexes if they exist
     b. If PostgreSQL, execute `GRANT UPDATE, DELETE ON audit_log TO vera`
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/ -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/app/services/audit_service.py` exists and contains `async def write(`
    - File `backend/app/services/audit_service.py` does NOT contain `await db.commit()`
    - File `backend/app/schemas/audit_log.py` contains both `class AuditLogOut` and `class AuditLogPageOut`
    - `AuditLogPageOut` has fields `items: list[AuditLogOut]` and `total: int`
    - Alembic migration has `down_revision = "j4k5l6m7n8o9"`
    - Migration contains `ix_audit_log_tenant_entity` and `ix_audit_log_tenant_created`
    - Migration contains `if conn.dialect.name == "postgresql":` guard before REVOKE
    - All existing backend tests still pass
  </acceptance_criteria>
  <done>audit_service.write() function exists and is importable, schemas defined, Alembic migration adds indexes idempotently and REVOKEs on PostgreSQL only</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test scaffolds for all AUDIT requirements</name>
  <files>
    backend/tests/test_audit.py
  </files>
  <read_first>
    backend/tests/conftest.py,
    backend/app/services/audit_service.py (just created in Task 1),
    backend/app/schemas/audit_log.py (just created in Task 1),
    backend/tests/test_payroll.py (first 50 lines for fixture patterns)
  </read_first>
  <behavior>
    - test_audit_service_write_stages_row: calling audit_service.write() + db.commit() creates an AuditLog row
    - test_audit_service_no_commit_without_caller: calling audit_service.write() alone (no commit) leaves no persisted row after rollback
    - test_audit_log_indexes: after migration, audit_log table has indexes ix_audit_log_tenant_entity and ix_audit_log_tenant_created (inspect check)
    - test_revoke_migration_sqlite_skip: the REVOKE line does not execute on SQLite (no error raised)
    - test_shift_create_produces_audit_row: POST /api/v1/shifts creates an AuditLog row with entity_type="shift" and action="create"
    - test_shift_delete_produces_audit_row: DELETE /api/v1/shifts/{id} creates an AuditLog row with action="delete"
    - test_employee_create_produces_audit_row: POST /api/v1/employees creates AuditLog with entity_type="employee"
    - test_employee_update_produces_audit_row: PUT /api/v1/employees/{id} creates AuditLog with action="update" and old_values is not None
    - test_payroll_calculate_produces_audit_row: POST /api/v1/payroll/calculate creates AuditLog with entity_type="payroll"
    - test_payroll_audit_before_after_fields: payroll update AuditLog has old_values/new_values containing keys "actual_hours", "base_wage", "total_gross"
    - test_absence_create_produces_audit_row: POST /api/v1/absences creates AuditLog with entity_type="absence"
    - test_contract_history_create_produces_audit_row: POST /api/v1/employees/{id}/contracts creates AuditLog with entity_type="contract_history"
    - test_rollback_no_orphan_audit_row: a mutation that raises an exception mid-transaction leaves zero AuditLog rows
    - test_audit_log_api_pagination: GET /api/v1/audit-log returns {items: [...], total: N} with correct pagination
    - test_audit_log_api_filters: GET /api/v1/audit-log?entity_type=shift filters correctly
    - test_audit_log_api_admin_only: GET /api/v1/audit-log with employee token returns 403
  </behavior>
  <action>
Create `backend/tests/test_audit.py` with all the test functions listed in behavior. Structure:

1. Import fixtures from conftest: `client`, `db`, `admin_token`, `employee_token`, `tenant`, `admin_user`
2. For tests that verify endpoint audit wiring (test_shift_create_produces_audit_row, test_employee_*, test_payroll_*, test_absence_*, test_contract_history_*): write the full test with HTTP call + AuditLog query, but mark them with `@pytest.mark.skip(reason="audit wiring not yet implemented")`. These will be unskipped in Plan 02 when wiring is done.
3. For tests that verify the audit_service directly (test_audit_service_write_stages_row, test_audit_service_no_commit_without_caller): implement fully — these should pass immediately.
4. For tests that verify the API endpoint (test_audit_log_api_*): write full tests but mark with `@pytest.mark.skip(reason="audit-log endpoint not yet implemented")`. These will be unskipped in Plan 03.
5. For test_audit_log_indexes: implement fully using `Inspector.from_engine()` — should pass after migration runs.
6. For test_revoke_migration_sqlite_skip: implement as a simple assertion that the migration `upgrade()` does not raise on SQLite.

Key patterns:
- Use `db.expire_all()` (NO await) after HTTP mutations before querying AuditLog
- Query AuditLog: `await db.execute(select(AuditLog).where(AuditLog.tenant_id == tenant.id, AuditLog.entity_type == "shift"))`
- Use `admin_token` for auth header: `headers={"Authorization": f"Bearer {admin_token}"}`
- For shift creation, POST to `/api/v1/shifts` with a valid ShiftCreate payload (needs employee_id — create one first or use fixture)
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_audit.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/test_audit.py` exists with at least 16 test functions
    - `grep -c "def test_" backend/tests/test_audit.py` returns 16 or more
    - `grep -c "pytest.mark.skip" backend/tests/test_audit.py` returns at least 10 (skipped tests for Plans 02 and 03)
    - Non-skipped tests (audit_service direct tests, index test, revoke test) all pass
    - `cd backend && python3 -m pytest tests/test_audit.py -v --tb=short` shows no failures for non-skipped tests
  </acceptance_criteria>
  <done>Test file has scaffolds for all AUDIT requirements; direct audit_service tests pass green; endpoint-wiring tests are skipped pending Plan 02/03 implementation</done>
</task>

</tasks>

<verification>
- `cd backend && python3 -m pytest tests/test_audit.py -v --tb=short` — non-skipped tests pass
- `cd backend && python3 -m pytest tests/ -q` — full suite still green
- `grep -r "async def write" backend/app/services/audit_service.py` — function exists
- `grep "ix_audit_log_tenant_entity" backend/alembic/versions/k5l6m7n8o9p0_*.py` — index in migration
</verification>

<success_criteria>
- audit_service.write() is importable and stages AuditLog rows without committing
- AuditLogOut and AuditLogPageOut schemas are defined
- Alembic migration creates composite indexes idempotently and REVOKEs on PostgreSQL only
- Test scaffolds exist for all AUDIT-01 through AUDIT-05 requirements
- Full backend test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-audit-trail/03-01-SUMMARY.md`
</output>
