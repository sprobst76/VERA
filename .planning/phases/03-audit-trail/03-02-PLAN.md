---
phase: 03-audit-trail
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/app/api/v1/shifts.py
  - backend/app/api/v1/employees.py
  - backend/app/api/v1/payroll.py
  - backend/app/api/v1/absences.py
  - backend/tests/test_audit.py
autonomous: true
requirements: [AUDIT-02, AUDIT-03]

must_haves:
  truths:
    - "Creating a shift produces an AuditLog row with entity_type=shift and action=create"
    - "Deleting a shift produces an AuditLog row with action=delete"
    - "Creating or updating an employee produces an AuditLog row with entity_type=employee"
    - "Creating a contract history entry produces an AuditLog row with entity_type=contract_history"
    - "Calculating payroll produces an AuditLog row with entity_type=payroll"
    - "Updating payroll captures actual_hours, base_wage, total_gross in old_values and new_values"
    - "Creating or updating an absence produces an AuditLog row with entity_type=absence"
    - "A rolled-back mutation produces no orphan AuditLog row"
  artifacts:
    - path: "backend/app/api/v1/shifts.py"
      provides: "Shift audit wiring for create + delete (update/confirm/claim already wired)"
      contains: "audit_service.write"
    - path: "backend/app/api/v1/employees.py"
      provides: "Employee + ContractHistory audit wiring"
      contains: "audit_service.write"
    - path: "backend/app/api/v1/payroll.py"
      provides: "Payroll audit wiring with before/after for AUDIT-03 fields"
      contains: "PAYROLL_AUDIT_FIELDS"
    - path: "backend/app/api/v1/absences.py"
      provides: "Absence audit wiring"
      contains: "audit_service.write"
  key_links:
    - from: "backend/app/api/v1/employees.py"
      to: "backend/app/services/audit_service.py"
      via: "import audit_service"
      pattern: "from app\\.services import audit_service"
    - from: "backend/app/api/v1/payroll.py"
      to: "backend/app/services/audit_service.py"
      via: "import audit_service"
      pattern: "from app\\.services import audit_service"
    - from: "backend/app/api/v1/absences.py"
      to: "backend/app/services/audit_service.py"
      via: "import audit_service"
      pattern: "from app\\.services import audit_service"
---

<objective>
Wire audit_service.write() into all write endpoints that currently lack audit coverage: employees, payroll, absences, and the remaining shift gaps (create + delete). Implement AUDIT-03 payroll before/after snapshots.

Purpose: Every create/update/delete on the five audited entity types must produce an AuditLog row in the same transaction. This is the core compliance requirement of Phase 3.
Output: All write endpoints emit audit entries; payroll entries capture actual_hours, base_wage, total_gross diffs; all previously-skipped tests unskipped and passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-audit-trail/03-RESEARCH.md
@.planning/phases/03-audit-trail/03-01-SUMMARY.md

@backend/app/services/audit_service.py
@backend/app/api/v1/shifts.py
@backend/app/api/v1/employees.py
@backend/app/api/v1/payroll.py
@backend/app/api/v1/absences.py
@backend/tests/test_audit.py

<interfaces>
<!-- audit_service.write() from Plan 01 -->
async def write(
    db: AsyncSession,
    *,
    tenant_id: uuid.UUID,
    user_id: uuid.UUID,
    entity_type: str,
    entity_id: uuid.UUID,
    action: str,
    old_values: dict | None = None,
    new_values: dict | None = None,
) -> None:
    # Stages AuditLog row — caller commits

<!-- PAYROLL_AUDIT_FIELDS constant for before/after snapshots -->
PAYROLL_AUDIT_FIELDS = ("actual_hours", "base_wage", "total_gross")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire audit_service.write() into shifts.py (create + delete) and refactor existing _write_audit</name>
  <files>backend/app/api/v1/shifts.py</files>
  <read_first>
    backend/app/api/v1/shifts.py,
    backend/app/services/audit_service.py
  </read_first>
  <action>
1. Add import at top of shifts.py: `from app.services import audit_service`

2. Replace the local `_write_audit` helper (lines 41-52) with calls to `audit_service.write()`. For each existing call site that currently calls `_write_audit(db, tenant_id=..., user_id=..., entity_id=..., action=..., ...)`, replace with `await audit_service.write(db, tenant_id=..., user_id=..., entity_type="shift", entity_id=..., action=..., ...)`. The key difference: add `entity_type="shift"` explicitly since the shared helper no longer hardcodes it.

3. Add audit to the `POST /shifts` create endpoint (the function `create_shift`):
   - After `db.add(shift)` and `await db.flush()` (to get shift.id), BEFORE `await db.commit()`:
   - Call `await audit_service.write(db, tenant_id=current_user.tenant_id, user_id=current_user.id, entity_type="shift", entity_id=shift.id, action="create", new_values=payload.model_dump(mode="json"))`
   - The `mode="json"` ensures dates/UUIDs are serialized as strings for JSON storage

4. Add audit to the `DELETE /shifts/{id}` endpoint (the function that handles shift deletion):
   - BEFORE `await db.delete(shift)`, capture: `old_values = {"employee_id": str(shift.employee_id), "date": str(shift.date), "start_time": str(shift.start_time), "end_time": str(shift.end_time), "status": shift.status}`
   - After delete and BEFORE `await db.commit()`: `await audit_service.write(db, tenant_id=current_user.tenant_id, user_id=current_user.id, entity_type="shift", entity_id=shift_id, action="delete", old_values=old_values)`

5. Delete the old `_write_audit` function definition entirely.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_audit.py -k "shift" -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_write_audit" backend/app/api/v1/shifts.py` returns 0 (old helper removed)
    - `grep -c "audit_service.write" backend/app/api/v1/shifts.py` returns at least 6 (create, delete, update, confirm, claim, time_correction calls)
    - `grep "from app.services import audit_service" backend/app/api/v1/shifts.py` matches
    - Tests test_shift_create_produces_audit_row and test_shift_delete_produces_audit_row pass (unskipped)
  </acceptance_criteria>
  <done>shifts.py uses shared audit_service.write() for all mutations including create and delete; local _write_audit is removed</done>
</task>

<task type="auto">
  <name>Task 2: Wire audit into employees.py, payroll.py, absences.py and unskip all tests</name>
  <files>
    backend/app/api/v1/employees.py,
    backend/app/api/v1/payroll.py,
    backend/app/api/v1/absences.py,
    backend/tests/test_audit.py
  </files>
  <read_first>
    backend/app/api/v1/employees.py,
    backend/app/api/v1/payroll.py,
    backend/app/api/v1/absences.py,
    backend/app/services/audit_service.py,
    backend/tests/test_audit.py
  </read_first>
  <action>
**employees.py** — add `from app.services import audit_service` at top, then:

1. `create_employee` (POST /employees): after `db.add(employee)` + `await db.flush()`, before `await db.commit()`:
   `await audit_service.write(db, tenant_id=current_user.tenant_id, user_id=current_user.id, entity_type="employee", entity_id=employee.id, action="create", new_values=payload.model_dump(mode="json"))`

2. `update_employee` (PUT /employees/{id}): BEFORE the setattr loop, capture old_values:
   `updates = payload.model_dump(exclude_unset=True)`
   `old_values = {f: str(getattr(employee, f)) for f in updates}`
   After all setattr calls, capture new_values:
   `new_values = {f: str(getattr(employee, f)) for f in updates}`
   Before `await db.commit()`:
   `await audit_service.write(db, tenant_id=current_user.tenant_id, user_id=current_user.id, entity_type="employee", entity_id=employee_id, action="update", old_values=old_values, new_values=new_values)`

3. `deactivate_employee` (DELETE /employees/{id}): before `await db.commit()`:
   `await audit_service.write(db, tenant_id=current_user.tenant_id, user_id=current_user.id, entity_type="employee", entity_id=employee_id, action="delete", old_values={"name": employee.name, "email": employee.email})`

4. `add_contract` (POST /employees/{id}/contracts): after `db.add(new_contract)` + `await db.flush()`, before `await db.commit()`:
   `await audit_service.write(db, tenant_id=current_user.tenant_id, user_id=current_user.id, entity_type="contract_history", entity_id=new_contract.id, action="create", new_values={"employee_id": str(employee_id), "contract_type": payload.contract_type, "hourly_rate": float(payload.hourly_rate) if payload.hourly_rate else None, "valid_from": str(effective_valid_from)})`

**payroll.py** — add `from app.services import audit_service` at top, then define:
`PAYROLL_AUDIT_FIELDS = ("actual_hours", "base_wage", "total_gross")`

1. `calculate_payroll` (POST /payroll/calculate):
   - If an existing draft is found before deletion, capture: `old_values = {f: float(getattr(existing, f)) if getattr(existing, f) is not None else None for f in PAYROLL_AUDIT_FIELDS}` BEFORE deleting the existing entry
   - After the new entry is created and flushed, before commit: if old_values existed, write an "update" audit; otherwise write a "create" audit
   - For create: `await audit_service.write(db, tenant_id=current_user.tenant_id, user_id=current_user.id, entity_type="payroll", entity_id=entry.id, action="create", new_values={f: float(getattr(entry, f)) if getattr(entry, f) is not None else None for f in PAYROLL_AUDIT_FIELDS})`
   - For update (replacing draft): use old_values captured above and new_values from new entry

2. `update_payroll_entry` (PUT /payroll/{id}):
   - BEFORE setattr loop: `old_values = {f: float(getattr(entry, f)) if getattr(entry, f) is not None else None for f in PAYROLL_AUDIT_FIELDS}`
   - AFTER setattr loop: `new_values = {f: float(getattr(entry, f)) if getattr(entry, f) is not None else None for f in PAYROLL_AUDIT_FIELDS}`
   - Before commit: `await audit_service.write(db, tenant_id=current_user.tenant_id, user_id=current_user.id, entity_type="payroll", entity_id=entry.id, action="update", old_values=old_values, new_values=new_values)`

**absences.py** — add `from app.services import audit_service` at top, then:

1. `create_absence` (POST /absences): after `db.add(absence)` + flush, before commit:
   `await audit_service.write(db, tenant_id=current_user.tenant_id, user_id=current_user.id, entity_type="absence", entity_id=absence.id, action="create", new_values=payload.model_dump(mode="json"))`

2. `update_absence` (PUT /absences/{id}): BEFORE update, capture:
   `updates = payload.model_dump(exclude_unset=True)`
   `old_values = {f: str(getattr(absence, f)) for f in updates}`
   After updates: `new_values = {f: str(getattr(absence, f)) for f in updates}`
   Before commit: `await audit_service.write(db, tenant_id=current_user.tenant_id, user_id=current_user.id, entity_type="absence", entity_id=absence.id, action="update", old_values=old_values, new_values=new_values)`

**test_audit.py** — Remove all `@pytest.mark.skip` decorators from endpoint-wiring tests:
   - test_shift_create_produces_audit_row
   - test_shift_delete_produces_audit_row
   - test_employee_create_produces_audit_row
   - test_employee_update_produces_audit_row
   - test_payroll_calculate_produces_audit_row
   - test_payroll_audit_before_after_fields
   - test_absence_create_produces_audit_row
   - test_contract_history_create_produces_audit_row
   - test_rollback_no_orphan_audit_row

Keep skip on test_audit_log_api_* tests (those are for Plan 03).
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_audit.py -k "not api" -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from app.services import audit_service" backend/app/api/v1/employees.py` matches
    - `grep "from app.services import audit_service" backend/app/api/v1/payroll.py` matches
    - `grep "from app.services import audit_service" backend/app/api/v1/absences.py` matches
    - `grep "PAYROLL_AUDIT_FIELDS" backend/app/api/v1/payroll.py` matches
    - `grep -c "pytest.mark.skip" backend/tests/test_audit.py` returns exactly 3 (only the API endpoint tests remain skipped)
    - All non-skipped audit tests pass: `cd backend && python3 -m pytest tests/test_audit.py -k "not api" -v` shows 0 failures
    - Full backend suite passes: `cd backend && python3 -m pytest tests/ -q`
  </acceptance_criteria>
  <done>All five entity types (shift, employee, payroll, absence, contract_history) produce AuditLog rows on create/update/delete; payroll audit entries contain actual_hours, base_wage, total_gross in before/after; no orphan rows on rollback</done>
</task>

</tasks>

<verification>
- `cd backend && python3 -m pytest tests/test_audit.py -k "not api" -v --tb=short` — all endpoint-wiring tests pass
- `cd backend && python3 -m pytest tests/ -q` — full suite green
- `grep -r "audit_service.write" backend/app/api/v1/` — calls present in shifts.py, employees.py, payroll.py, absences.py
</verification>

<success_criteria>
- Every write endpoint for Shift, Employee, PayrollEntry, ContractHistory, EmployeeAbsence calls audit_service.write() before db.commit()
- Payroll audit entries include actual_hours, base_wage, total_gross as float values in old_values and new_values
- Rolled-back transactions produce no orphan audit rows
- All audit tests (except API endpoint tests) pass green
</success_criteria>

<output>
After completion, create `.planning/phases/03-audit-trail/03-02-SUMMARY.md`
</output>
