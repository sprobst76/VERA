name: vera

services:

  # ── PostgreSQL ───────────────────────────────────────────────────────────────
  vera-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: vera
      POSTGRES_USER: vera
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    command: >
      postgres
      -c shared_buffers=32MB
      -c work_mem=4MB
      -c maintenance_work_mem=32MB
      -c max_connections=25
      -c effective_cache_size=128MB
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vera -d vera"]
      interval: 10s
      timeout: 5s
      retries: 5
    mem_limit: 256m
    restart: unless-stopped
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    networks:
      - default

  # ── Redis (Celery Broker + Backend) ──────────────────────────────────────────
  vera-redis:
    image: redis:7-alpine
    restart: unless-stopped
    mem_limit: 64m
    volumes:
      - redis_data:/data
    logging:
      driver: json-file
      options: { max-size: "5m", max-file: "2" }
    networks:
      - default

  # ── Backend (FastAPI) ────────────────────────────────────────────────────────
  vera-api:
    image: ghcr.io/sprobst76/vera-backend:latest
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://vera:${POSTGRES_PASSWORD}@vera-db:5432/vera
    depends_on:
      vera-db:
        condition: service_healthy
    volumes:
      - ./data/backups:/app/backups
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    mem_limit: 256m
    restart: unless-stopped
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    labels:
      - "traefik.enable=true"
      # API + calendar + health – höhere Priorität als Frontend
      - "traefik.http.routers.vera-api.rule=Host(`vera.lab.${DOMAIN}`) && (PathPrefix(`/api`) || PathPrefix(`/calendar/`) || PathPrefix(`/health`))"
      - "traefik.http.routers.vera-api.entrypoints=websecure"
      - "traefik.http.routers.vera-api.tls.certresolver=cloudflare"
      - "traefik.http.routers.vera-api.priority=20"
      - "traefik.http.routers.vera-api.middlewares=vera-ratelimit@docker,vera-security@docker"
      - "traefik.http.services.vera-api.loadbalancer.server.port=8000"
      # Rate limiting (Brute-Force-Schutz für /auth/login)
      - "traefik.http.middlewares.vera-ratelimit.ratelimit.average=30"
      - "traefik.http.middlewares.vera-ratelimit.ratelimit.burst=10"
      - "traefik.http.middlewares.vera-ratelimit.ratelimit.period=1m"
      # Security headers
      - "traefik.http.middlewares.vera-security.headers.stsSeconds=31536000"
      - "traefik.http.middlewares.vera-security.headers.stsIncludeSubdomains=true"
      - "traefik.http.middlewares.vera-security.headers.contentTypeNosniff=true"
      - "traefik.http.middlewares.vera-security.headers.frameDeny=true"
    networks:
      - default
      - ai-lab

  # ── Celery Worker ────────────────────────────────────────────────────────────
  vera-celery-worker:
    image: ghcr.io/sprobst76/vera-backend:latest
    command: celery -A app.tasks.celery_app worker --loglevel=info --concurrency=2
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://vera:${POSTGRES_PASSWORD}@vera-db:5432/vera
      REDIS_URL: redis://vera-redis:6379/0
    depends_on:
      vera-db:
        condition: service_healthy
      vera-redis:
        condition: service_started
    mem_limit: 256m
    restart: unless-stopped
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    networks:
      - default

  # ── Celery Beat (Scheduler) ───────────────────────────────────────────────────
  vera-celery-beat:
    image: ghcr.io/sprobst76/vera-backend:latest
    command: celery -A app.tasks.celery_app beat --loglevel=info
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://vera:${POSTGRES_PASSWORD}@vera-db:5432/vera
      REDIS_URL: redis://vera-redis:6379/0
    depends_on:
      vera-redis:
        condition: service_started
    mem_limit: 64m
    restart: unless-stopped
    logging:
      driver: json-file
      options: { max-size: "5m", max-file: "2" }
    networks:
      - default

  # ── Frontend (Next.js Standalone) ────────────────────────────────────────────
  vera-web:
    image: ghcr.io/sprobst76/vera-frontend:latest
    depends_on:
      - vera-api
    mem_limit: 128m
    restart: unless-stopped
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    labels:
      - "traefik.enable=true"
      # Frontend – niedrigere Priorität als API
      # Demo-Zugang via /{DEMO_SLUG}/ – kein eigener Hostname, kein CT-Log-Eintrag
      - "traefik.http.routers.vera-web.rule=Host(`vera.lab.${DOMAIN}`)"
      - "traefik.http.routers.vera-web.entrypoints=websecure"
      - "traefik.http.routers.vera-web.tls.certresolver=cloudflare"
      - "traefik.http.routers.vera-web.priority=10"
      - "traefik.http.routers.vera-web.middlewares=vera-web-security@docker"
      - "traefik.http.services.vera-web.loadbalancer.server.port=3000"
      # Security headers (eigenständig definiert, nicht von vera-api abhängig)
      - "traefik.http.middlewares.vera-web-security.headers.stsSeconds=31536000"
      - "traefik.http.middlewares.vera-web-security.headers.stsIncludeSubdomains=true"
      - "traefik.http.middlewares.vera-web-security.headers.contentTypeNosniff=true"
      - "traefik.http.middlewares.vera-web-security.headers.frameDeny=true"
    networks:
      - default
      - ai-lab

networks:
  ai-lab:
    external: true
    name: ai-lab_ai-lab   # Docker Compose project prefix
  default:
    driver: bridge

volumes:
  redis_data:
